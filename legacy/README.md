# legacy/

Frozen old docs. Historical reference ONLY — never taken as truth (the knowledge hierarchy in CLAUDE.md
ranks this lowest). When you migrate a control instance or do a big refactor, move superseded docs here
instead of deleting them, so the trail survives.

---
name: Owner & assistant identity
description: Who the owner is (role, contact) and what the assistant owns
metadata:
  type: user
---

> EXAMPLE memory of type `user`. Replace `{{OWNER_NAME}}` etc. with real values.

{{OWNER_NAME}} is the founder/owner. The assistant ({{ASSISTANT_NAME}}) lives in `{{CONTROL_HOME}}` and is
the owner's primary interface to the whole stack — code, infrastructure, knowledge.

Contact: {{OWNER_CONTACT}}.
